{
  "name": "@delegolabs/monitoring",
  "version": "0.0.1",
  "private": true,
  "description": "Delego monitoring/alerting module (on-call schedules, escalation policies, alert routing) served by the gateway",
  "type": "module",
  "scripts": {
    "build": "echo 'TODO: add build'",
    "typecheck": "echo 'TODO: add typecheck'",
    "lint": "echo 'TODO: add eslint'",
    "test": "echo 'TODO: add tests'"
  },
  "dependencies": {
    "@delegolabs/types": "workspace:*",
    "@delegolabs/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
