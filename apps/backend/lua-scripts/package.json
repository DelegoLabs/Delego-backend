{
  "name": "@delegolabs/lua-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "node --import tsx --watch src/index.ts",
    "build": "echo 'TODO: add build'",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'TODO: add eslint'",
    "typecheck": "echo 'TODO: add typecheck'"
  },
  "dependencies": {
    "@delegolabs/types": "workspace:*",
    "@delegolabs/utils": "workspace:*",
    "ioredis": "^5.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
